
function newTheme()
	return {
		-- Border
		borderSize=0,
		borderSizeHover=1,
		borderColor=colors.DARK_GRAY,
		borderColorHover=colors.GRAY,
		-- Fill colors
		color=colors.GRAY,
		colorHover=colors.LIGHT_GRAY,
		-- Text
		font=nil,
		textSize=2,
		textColor=colors.LIGHT_GRAY,
		textColorHover=colors.WHITE,
		-- Space
		padding=16
	}
end